# Nosana MCP Server Configuration

# ─── Authentication ───────────────────────────────────
# Users can connect with EITHER a Solana wallet OR a Nosana API key.
#
# Option 1: Wallet (Phantom) — Users connect via browser wallet extension.
#   No server-side API key needed for wallet-based operations.
#
# Option 2: Nosana API Key — Users provide their API key (nos_xxx_...).
#   Get your API key at: https://deploy.nosana.com
#   Docs: https://learn.nosana.com/api/intro.html
#
# Server-side fallback API key (used when SDK methods fail):
NOSANA_API_KEY=

# Redis URL for MCP Handler
# REDIS_URL=redis://localhost:6379

# Environment
NODE_ENV=development
